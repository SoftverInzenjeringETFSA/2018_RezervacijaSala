import { StyleSheet } from 'react-native';

export default styles = StyleSheet.create({

  kreirajSaluHeader:{
    textAlign: 'left',
    paddingTop: 20,
    paddingBottom: 10,
    paddingLeft: 5,
    fontSize: 20,
    backgroundColor: '#42b0e5',
    color: 'white',
    fontWeight: 'bold'
  },

  submitButton:{
    textAlign: 'center',
    position: 'absolute',
    bottom: 0,
    left: 0
  }
});
